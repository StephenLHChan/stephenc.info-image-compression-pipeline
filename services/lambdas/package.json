{
  "name": "image-compression-lambda",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "test:watch": "jest --watch",
    "deploy": "npm run build && mkdir -p dist && cp package.json dist/ && cd dist && npm install --omit=dev --os=linux --cpu=x64"
  },
  "dependencies": {
    "sharp": "^0.33.5",
    "@aws-sdk/client-s3": "^3.679.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.8.1",
    "typescript": "^5.3.3",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8",
    "ts-jest": "^29.1.1"
  }
}
